this project is running a simple animatedScroll in bootstrap, it has stylus installed the styles.style imports the other style functions and methods and mixins and creates a styles.css
bootstrap is called into the html, so is the stylesheet and some google fonts, stylus has nib and typographic extentions
yarn is handeling the node dependencies, package paths are found using (./*)
Gulp is creating the project as we go
